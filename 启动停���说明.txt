���后随���管���系统 - 启动与停���说明
=====================================

一���系统启���步���
----------------

### 1. 启动后端服���器（Django）

步骤：
1) ���开���令行窗���（CMD或PowerShell）
2) 进���后端目录：
   cd d:\desktop\document\软���\new\backend

3) 激活Python虚���环境（如果使���虚拟环���）：
   venv\Scripts\activate

4) 启动Django开���服务器：
   python manage.py runserver 8000

5) 看到���下提示表示启动成���：
   "Starting development server at http://127.0.0.1:8000/"

### 2. 启动前���服务器���Vue）

步骤：
1) 打开���的命���行窗口
2) 进���前端目录：
   cd d:\desktop\document\软著\new\frontend

3) 安���依赖���仅首次运���需���）：
   npm install

4) ���动Vue开发服务器：
   npm run dev

5) 看到以下提示表示启���成功：
   "Local: http://localhost:5173/"

### 3. 访问系统

在浏览���中���开：http://localhost:5173

测���账���：
- 管理员���admin / admin123
- 医生：doctor1 / 123456
- 患���：patient1 / 123456


���、系统停止步骤
----------------

### 停止后端服务器

在运行���端的���令行窗���中：
- ��� Ctrl + C 组合���
- 等待���示"Quit the server with CONTROL-C"后服务停止
- ���闭命令行窗口

### 停���前端服务器

在运行���端���命令行���口中：
- 按 Ctrl + C 组合键
- ���果提示"Terminate batch job (Y/N)?"，输入 Y 并���车
- 关���命令���窗���


三、���见问���处理
----------------

### 问题1���端口被占用
如果提示"端口8000已被使���"或"���口5173已被���用"：

���决���法：
1) 查找占用���口的进程：
   netstat -ano | findstr "8000"    （后端���
   netstat -ano | findstr "5173"    （���端���

2) ���束占用进程：
   taskkill /F /PID <进程ID>

3) ���新启动���务器

### ���题2：数据库文���缺失
���果后端报错"no such table"：

���决���法：
1) 进���backend目录
2) 运���数据���迁移���
   python manage.py migrate

3) 创建���级���理员（���选）���
   python manage.py createsuperuser

### 问���3：前端���法���接后端
确认：
1) 后端服务器已启���（http://localhost:8000）
2) ���端配置���API地址正���
3) ���览器���制台无CORS错误

### 问题4：依赖包缺���
后端：
   pip install -r requirements.txt

前端：
   npm install


四、���发���境要求
----------------

后���要���：
- Python 3.8+
- Django 5.0.1
- Django REST Framework

前端要���：
- Node.js 16+
- npm ��� yarn

数据���：
- SQLite（开���环境���无需额���安装）


五、注意���项
------------

1. 必���先启���后端服务器，再启动前���服���器
2. 两���服务器必���同时运���才能正常���用系统
3. 关闭系统时���议先停止前端，���停止后���
4. ���据库文���位于：backend/db.sqlite3
5. 首次运行请确保���安装所有���赖���
6. 生产���境部署���要额外配置（请参考部���文档）


六、快速启动脚本���可���）
------------------------

可以创建批处���文件实���一键启动：

启动脚���（start.bat）���
@echo off
start cmd /k "cd backend && python manage.py runserver 8000"
timeout /t 3
start cmd /k "cd frontend && npm run dev"

停止方法���
在���个���令行窗口���分别按 Ctrl+C


===========================================
如有问题，请查���项目���录下的 README.md ���件
或联���系���管理员
===========================================
