���后随访管理系统 - 启动与停���说明
=====================================

一、���统���动步���
----------------

### 1. 启动���端服���器（Django）

步骤：
1) ���开命令行窗口���CMD���PowerShell）
2) 进入���端目录���
   cd d:\desktop\document\���著\new\backend

3) 激活Python虚拟环境���如���使用虚拟环境）：
   venv\Scripts\activate

4) 启���Django开���服务器：
   python manage.py runserver 8000

5) 看到���下提���表示启动成功：
   "Starting development server at http://127.0.0.1:8000/"

### 2. 启动前端���务器���Vue）

���骤：
1) 打开新的���令行���口
2) 进���前端目录：
   cd d:\desktop\document\���著\new\frontend

3) 安���依赖（仅首次运行需���）：
   npm install

4) 启动Vue���发服务���：
   npm run dev

5) 看���以下���示表示启动成功：
   "Local: http://localhost:5173/"

### 3. 访问���统

在浏览器���打���：http://localhost:5173

测���账号���
- 管理员：admin / admin123
- 医生：doctor1 / 123456
- 患���：patient1 / 123456


���、系���停止步骤
----------------

### ���止后端服���器

在���行后端的命令行窗口中：
- 按 Ctrl + C 组合键
- ���待提���"Quit the server with CONTROL-C"后服务停止
- 关���命令行���口

### ���止前端服务器

在运���前���的命���行窗口���：
- 按 Ctrl + C 组合���
- 如果���示"Terminate batch job (Y/N)?"，输入 Y ���回车
- 关闭命令���窗口


三、常见���题处理
----------------

### 问题1���端口���占用
如果提示"端���8000已被使用"或"端口5173已被使用"：

���决方法：
1) ���找占用���口的进���：
   netstat -ano | findstr "8000"    （���端）
   netstat -ano | findstr "5173"    （前端）

2) 结束占���进程：
   taskkill /F /PID <进程ID>

3) 重新���动服务器

### 问题2：数据���文件缺���
���果后端报错"no such table"：

解���方���：
1) ���入backend���录
2) 运���数据库���移：
   python manage.py migrate

3) 创���超级管理员（可���）：
   python manage.py createsuperuser

### 问题3：前端无���连���后���
确认：
1) 后端服���器已启动（http://localhost:8000）
2) 前端���置中API地址正���
3) 浏览���控制台无CORS错误

### 问���4：依赖���缺失
���端：
   pip install -r requirements.txt

前端���
   npm install


四、开发环境要求
----------------

���端要���：
- Python 3.8+
- Django 5.0.1
- Django REST Framework

前端要求���
- Node.js 16+
- npm 或 yarn

���据库：
- SQLite（开发环境���无需���外安装���


五���注意事项
------------

1. 必须先启动后���服���器，���启动前端服���器
2. 两���服务器必���同时运���才���正常使���系统
3. 关闭���统时建议先停止前端，再停止���端
4. 数据库文件位于：backend/db.sqlite3
5. 首次运行请确保已���装所���依赖���
6. 生���环境部���需要额���配���（请参考���署文���）


六、快���启动脚���（可���）
------------------------

可以创建���处理文���实现一���启���：

���动脚本（start.bat）���
@echo off
start cmd /k "cd backend && python manage.py runserver 8000"
timeout /t 3
start cmd /k "cd frontend && npm run dev"

停止方���：
在两个命令���窗���中分别��� Ctrl+C


===========================================
���有问���，请查看项目���录下��� README.md 文件
���联系���统���理员
===========================================
