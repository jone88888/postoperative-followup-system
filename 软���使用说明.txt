术后���访���理系统 - 启动���停止说明
=====================================

一、系统启���步骤
----------------

### 1. 启动后端服���器（Django���

步���：
1) ���开���令���窗口（CMD或PowerShell）
2) 进入后端目录：
   cd d:\desktop\document\软���\new\backend

3) 激活Python虚拟环境（如果使���虚拟环���）：
   venv\Scripts\activate

4) 启动Django开发���务器：
   python manage.py runserver 8000

5) 看到以下提���表示���动成���：
   "Starting development server at http://127.0.0.1:8000/"

### 2. 启���前端���务器（Vue���

步骤���
1) ���开新���命令行窗口
2) 进入前端���录：
   cd d:\desktop\document\软著\new\frontend

3) 安装依赖（���首次���行���要）���
   npm install

4) 启动Vue开发服务器：
   npm run dev

5) 看���以下提示表示启动成功：
   "Local: http://localhost:5173/"

### 3. 访问系统

在���览���中打���：http://localhost:5173

测试账���：
- 管���员���admin / admin123
- 医生：doctor1 / 123456
- 患者：patient1 / 123456


二、系统停���步���
----------------

### ���止后端服务器

在运行后端的命令行窗口中���
- 按 Ctrl + C 组合键
- 等待提���"Quit the server with CONTROL-C"后���务停止
- 关���命令行���口

### 停止前端���务器

在运���前端的命令行���口中���
- 按 Ctrl + C ���合���
- 如���提示"Terminate batch job (Y/N)?"，输入 Y ���回车
- 关闭���令行���口


三���常���问题处理
----------------

### 问题1：���口被占���
如���提示"端���8000已���使用"���"端口5173已���使用"：

解决方法：
1) 查找占用���口的进���：
   netstat -ano | findstr "8000"    ���后端）
   netstat -ano | findstr "5173"    （前���）

2) 结束占���进程���
   taskkill /F /PID <进程ID>

3) 重新���动���务器

### 问���2：数据库文件缺���
如���后端报���"no such table"：

解决方���：
1) 进���backend目���
2) 运行数据���迁移���
   python manage.py migrate

3) 创建超级管理员���可选���：
   python manage.py createsuperuser

### 问题3：���端无法���接���端
确认���
1) 后端服务器已启动（http://localhost:8000）
2) 前端配置中API地���正确
3) 浏览器���制台无CORS错误

### 问题4：依���包缺失
后端���
   pip install -r requirements.txt

前端���
   npm install


���、开���环���要���
----------------

���端要求：
- Python 3.8+
- Django 5.0.1
- Django REST Framework

前端要���：
- Node.js 16+
- npm 或 yarn

数据库：
- SQLite（开发环境，无���额外安装）


五、注意���项
------------

1. 必须先启动���端服务器，再���动前���服���器
2. 两个服务器���须同时运���才能正常使���系统
3. 关闭系统时建议先停止前���，再停止后���
4. 数据���文件位于：backend/db.sqlite3
5. 首次运行���确保已安装所有依赖���
6. 生产���境部署需要额���配置���请参���部���文档）


六、快���启动���本（可选���
------------------------

可���创建���处理文件���现���键启动：

启动脚本���start.bat）���
@echo off
start cmd /k "cd backend && python manage.py runserver 8000"
timeout /t 3
start cmd /k "cd frontend && npm run dev"

停止方法：
在���个命令行���口中分别��� Ctrl+C


===========================================
���有问题，���查看项���目录下��� README.md 文件
或���系���统���理员
===========================================
